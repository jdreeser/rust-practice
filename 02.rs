fn main() {
    // Inline comment
    /*
    It's all C style, so you know what's up
    */
    // Block style comments let us switch expression execution up
    // like the following
    let x = 5 + /* 90 + */ 5;
    println!("Is `x` 10 or 100? x = {}", x);
}